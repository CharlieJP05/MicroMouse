#include "main.h"
#include "Structs.h"

#define NORTH 0
#define EAST 1
#define SOUTH 2
#define WEST 3


void Rotate (int x,int y,int [][] path ){
    int heading = NORTH;

    vector current = {0,0}; 
    vector target = {path};

    int current = pos[x][y]; 

    for (i=0)
        int current.x = path.x[i];
        int current.y = path.y[i];
        int target.x = path.x[i+1];
        int target.y = path.y[i+1];



    switch ()
    {
    case constant expression:
        /* code */
        break;
    
    default:
        break;
    }
}

void move(){
    
}